name: parkutem_app
description: ParkUTeM mobile application.
publish_to: "none"

version: 1.0.10+10
environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  flutter_local_notifications: ^21.0.0

  flutter_dotenv: ^6.0.1
  supabase_flutter: ^2.12.4
  url_launcher: ^6.3.2
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^9.2.4
  flutter_map: 8.2.2
  latlong2: 0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/images/